# Previous standalone main keymap

`adv360.keymap` is preserved unchanged from the former `main` tip,
`6b303df76d7e5e5f3fda7b54af247d913258241e`. That branch had a separate history
and contained only this file, without the complete firmware build setup.

The active, buildable configuration is now `config/adv360.keymap`. This archived
file is a historical reference, not an additional active layout. Both histories
are retained by the merge; no branch history was force-replaced.
